class Paladin extends Character {
  constructor(hp = 16, dmg = 3, mana = 160, name = 'Ulder', status = 'Alive') {
    super(hp, dmg, mana, name, status);
    this.healingLighting = healingLighting;
  };
};

